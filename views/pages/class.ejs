<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/head %>
</head>

<body>
    <% include ../partials/header %>
    <div class="container">
        <table class="table">
            <thead>
                <tr>
                    <th>SubjectID</th>
                    <th>Subjectname</th>
                    <th>Room</th>
                    <th>Schedule</th>
                    <th>Lecturer</th>
                </tr>
            </thead>
            <tbody>
                        <!-- Class in java script shount be ant key for data -->
                <% classes.forEach(function(thisclasses){  %>
                <tr>
                    <td><a href="/classdetail/<%= thisclasses.subject_id %>"><%= thisclasses.subject_id %></a></td>
                    <td><%= thisclasses.subject_name %></td>
                    <td><%= thisclasses.room %></td>
                    <td><%= thisclasses.schedule %></td>
                    <td><%= thisclasses.lecturer %></td>
                    <td><a name="" id="" class="btn btn-primary" href="/classedit/<%= thisclasses.subject_id %>" role="button">Edit</a></td>
                    <td><a name="" id="" class="btn btn-primary" href="/classdel/<%= thisclasses.subject_id %>" role="button">Delete</a></td>
                </tr>
                <% }); %></h2>


            </tbody>
        </table>
    </div>
    
    <center><a name="" id="" class="btn btn-primary" href="/classAdd" role="button">Add new Class</a></center>
    <footer>
        <% include ../partials/footer %>
    </footer>

</body>

</html>