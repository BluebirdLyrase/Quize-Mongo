<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/head %>
</head>

<body>
    <% include ../partials/header %>
    <div class="container">
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Full name</th>
                    <th>Major</th>
                </tr>
            </thead>
            <tbody>

                <% thisdata.students.forEach(function(s){  %>
                <tr>
                    <td><%= s.sid %></td>
                    <td><%= s.name %></td>
                    <td><%= s.major %></td>
                </tr>
                <% }); %></h2>



            </tbody>
        </table>
    </div>
    <footer>
        <% include ../partials/footer %>
    </footer>

</body>

</html>